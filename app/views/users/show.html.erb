<% provide( :title, @user.name) %>

<div class="row">
	<aside class="span4">
		<section>
			<h1>
				<span><%= gravatar_for @user %></span><br><br><br><br><br><br><br><br>
				<span><%= @user.name %></span>
				<span><%= @user.email %></span>
				</td>
			</h1>
		</section>
	</aside>
	<div class="span8">
	<% if @user.microposts.any? %>
		<h3>Microposts (<%= @user.microposts.count %>) </h3>
		<ol class="microposts">
		<%= render @microposts ,collection: current_user %>
		</ol>
		
		<% end %>
	</div>
</div>

